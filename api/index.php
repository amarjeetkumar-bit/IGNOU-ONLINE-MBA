<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="images/favicon.png" sizes="32x32" />
    <title>IGNOU Online MBA Admission 2026 | Fees, Eligibility, Apply Now</title>
    <meta name="description" content="Apply for IGNOU Online MBA Admission 2026. Check fees, eligibility, course details & admission process. Get free counselling and start your MBA.">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600;9..144,700;9..144,900&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=IBM+Plex+Mono:wght@500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --ink: #231436;
            --ink-2: #3a1f52;
            --maroon: #7a1e3a;
            --gold: #e0a03a;
            --gold-deep: #c07f1e;
            --indigo: #5b53d6;
            --indigo-deep: #453cc0;
            --blue: #2563eb;
            --parchment: #faf6ef;
            --card: #ffffff;
            --slate: #544a5c;
            --line: #eadfd2;
            --ok: #1f7a52;
            --shadow: 0 18px 50px -20px rgba(35, 20, 54, .32);
            --shadow-sm: 0 8px 24px -14px rgba(35, 20, 54, .35);
            --r: 18px;
            --maxw: 1180px;
        }

        * { box-sizing: border-box; margin: 0; padding: 0 }
        html { scroll-behavior: smooth }
        body {
            font-family: "Plus Jakarta Sans", system-ui, sans-serif;
            color: var(--ink); background: var(--parchment); line-height: 1.6;
            -webkit-font-smoothing: antialiased; overflow-x: hidden;
        }
        img { max-width: 100%; display: block }
        a { color: inherit; text-decoration: none }
        .wrap { width: 100%; max-width: var(--maxw); margin: 0 auto; padding: 0 20px }
        .eyebrow {
            font-family: "IBM Plex Mono", monospace; font-size: 12px; letter-spacing: .18em;
            text-transform: uppercase; color: var(--maroon); font-weight: 600;
        }
        h1, h2, h3, h4 { font-family: "Fraunces", serif; line-height: 1.1; font-weight: 600 }

        .btn {
            display: inline-flex; align-items: center; gap: 8px; justify-content: center;
            font-weight: 700; font-size: 15px; border: none; cursor: pointer;
            padding: 13px 24px; border-radius: 999px;
            transition: transform .15s ease, box-shadow .2s ease, background .2s ease; font-family: inherit;
        }
        .btn:focus-visible { outline: 3px solid var(--gold); outline-offset: 2px }
        .btn-gold { background: linear-gradient(180deg, var(--gold), var(--gold-deep)); color: #3a2405; box-shadow: var(--shadow-sm) }
        .btn-gold:hover { transform: translateY(-2px) }
        .btn-indigo { background: linear-gradient(180deg, var(--indigo), var(--indigo-deep)); color: #fff; box-shadow: var(--shadow-sm) }
        .btn-indigo:hover { transform: translateY(-2px) }
        .btn-ink { background: var(--ink); color: #fff }
        .btn-ink:hover { transform: translateY(-2px); background: var(--ink-2) }
        .btn-outline { background: transparent; border: 1.5px solid var(--line); color: var(--ink) }
        .btn-outline:hover { border-color: var(--maroon); color: var(--maroon) }
        .btn-ghost-light { background: rgba(255, 255, 255, .12); color: #fff; border: 1.5px solid rgba(255, 255, 255, .35) }
        .btn-ghost-light:hover { background: rgba(255, 255, 255, .22) }

        section { padding: 74px 0; position: relative }
        .sec-head { max-width: 640px; margin: 0 auto 44px; text-align: center }
        .sec-head h2 { font-size: clamp(28px, 4vw, 42px); margin: 10px 0 8px }
        .sec-head p { color: var(--slate); font-size: 16px }

        /* ---------- HEADER ---------- */
        header {
            position: sticky; top: 0; z-index: 60; background: rgba(255, 255, 255, .86);
            backdrop-filter: blur(10px); border-bottom: 1px solid var(--line); transition: box-shadow .2s;
        }
        header.scrolled { box-shadow: 0 6px 24px -18px rgba(35, 20, 54, .5) }
        .nav { display: flex; align-items: center; justify-content: space-between; height: 72px; gap: 16px }
        .brand { display: flex; align-items: center; gap: 10px; font-weight: 800 }
        .brand img { height: 42px; width: auto }
        .brand .fallback { font-family: "Fraunces", serif; font-size: 20px; color: var(--maroon) }
        .nav-links { display: flex; align-items: center; gap: 26px; list-style: none }
        .nav-links a { font-size: 15px; font-weight: 600; color: var(--ink); opacity: .85; transition: opacity .15s }
        .nav-links a:hover { opacity: 1; color: var(--maroon) }
        .nav-cta { display: flex; align-items: center; gap: 12px }
        .hamburger { display: none; background: none; border: none; cursor: pointer; padding: 8px }
        .hamburger span { display: block; width: 24px; height: 2.5px; background: var(--ink); border-radius: 2px; transition: .3s; margin: 5px 0 }
        .hamburger.open span:nth-child(1) { transform: translateY(7.5px) rotate(45deg) }
        .hamburger.open span:nth-child(2) { opacity: 0 }
        .hamburger.open span:nth-child(3) { transform: translateY(-7.5px) rotate(-45deg) }

        /* ---------- HERO (image background, screenshot style) ---------- */
        .hero {
            position: relative; min-height: clamp(560px, 70vh, 700px);
            display: flex; align-items: center; color: #fff; overflow: hidden; padding: 0;
        }
        .hero-bg {
            position: absolute; inset: 0; z-index: 0; background-size: cover; background-position: center right;
            background-image: url("images/banner.webp"), linear-gradient(120deg, #0b2a4a, #16324f 40%, #3a1f52);
        }
        .hero-overlay {
            position: absolute; inset: 0; z-index: 1;
            background: linear-gradient(90deg, rgba(8, 10, 25, .86) 0%, rgba(8, 10, 25, .62) 38%, rgba(8, 10, 25, .15) 62%, rgba(8, 10, 25, 0) 82%);
        }
        .hero-inner { position: relative; z-index: 2; width: 100%; display: grid; grid-template-columns: 1fr; padding: 56px 20px }
        .hero-copy { max-width: 620px }
        .hero-copy h1 {
            font-family: "Plus Jakarta Sans", sans-serif; font-weight: 800;
            font-size: clamp(34px, 5.4vw, 60px); line-height: 1.05; letter-spacing: -.01em;
            text-shadow: 0 2px 20px rgba(0, 0, 0, .35);
        }
        .course-pills { display: flex; flex-wrap: wrap; gap: 10px; margin: 18px 0 4px }
        .pill {
            display: inline-flex; align-items: center; background: var(--blue); color: #fff;
            border-radius: 999px; padding: 6px 16px; font-weight: 700; font-size: 13.5px;
            box-shadow: 0 6px 16px -8px rgba(37, 99, 235, .8);
        }
        .naac { margin: 16px 0 6px; font-size: 16px; font-weight: 600; color: rgba(255, 255, 255, .95) }
        .hero-cta { display: flex; flex-wrap: wrap; gap: 14px; margin-top: 16px }
        .scholarship-text { margin-top: 24px; font-family: "Fraunces", serif; font-weight: 700; font-size: clamp(19px, 2.4vw, 24px) }
        .closing { margin-top: 4px; font-weight: 700; font-size: 15px; color: rgba(255, 255, 255, .92) }

        /* ---------- COUNTDOWN ---------- */
        .countdown { display: flex; align-items: flex-start; gap: 10px; margin-top: 14px }
        .cd-unit { display: flex; flex-direction: column; align-items: center; gap: 7px }
        .cd-box {
            background: #fff; border-radius: 12px; min-width: 58px; height: 58px;
            display: grid; place-items: center; box-shadow: 0 10px 24px -10px rgba(0, 0, 0, .45);
        }
        .cd-box span { font-family: "Fraunces", serif; font-weight: 700; font-size: 25px; color: var(--maroon) }
        .cd-unit small { font-size: 11px; letter-spacing: .12em; color: #fff; font-weight: 700 }
        .cd-colon { color: #fff; font-size: 26px; font-weight: 700; margin-top: 14px }

        /* ---------- LEAD CARD / FORM ---------- */
        .lead-card {
            background: var(--card); border-radius: 22px; padding: 28px; box-shadow: var(--shadow);
            border: 1px solid var(--line); position: relative;
        }
        .lead-card::before {
            content: ""; position: absolute; inset: 0; border-radius: 22px; padding: 1.5px;
            background: linear-gradient(160deg, var(--gold), transparent 40%);
            -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
            -webkit-mask-composite: xor; mask-composite: exclude; pointer-events: none;
        }
        .lead-card h3 { font-size: 15px; margin-bottom: 4px; color: var(--ink) }
        .lead-card .sub { color: var(--slate); font-size: 12px; margin-bottom: 5px }
        .lead-perks {
            display: flex;
            gap: 10px;
            flex-wrap: nowrap;   /* was wrap — this forces one line */
            margin-bottom: 18px;
        }
        .lead-perks span { display: inline-flex; align-items: center; gap: 6px; font-size: 13px; font-weight: 600; color: var(--ink-2) }
        .lead-perks span::before { content: "✓"; color: var(--ok); font-weight: 800 }
        .field { margin-bottom: 12px }
        .field input, .field select, .field textarea {
            width: 100%; padding: 13px 15px; border: 1.5px solid var(--line); border-radius: 12px;
            font-family: inherit; font-size: 15px; color: var(--ink); background: #fdfbf7; transition: border .15s;
        }
        .field textarea { resize: vertical; min-height: 84px; line-height: 1.5 }
        .field input:focus, .field select:focus, .field textarea:focus { outline: none; border-color: var(--maroon) }
        .field .err { color: var(--maroon); font-size: 12.5px; margin-top: 5px; display: none }
        .field.invalid .err { display: block }
        .field.invalid input, .field.invalid select, .field.invalid textarea { border-color: var(--maroon) }
        .lead-card .btn { width: 100%; margin-top: 6px }
        .form-note { font-size: 12px; color: var(--slate); text-align: center; margin-top: 12px }
        .form-success { display: none; text-align: center; padding: 24px 8px }
        .form-success.show { display: block }
        .form-view.hide { display: none }
        .form-success .tick {
            width: 56px; height: 56px; border-radius: 50%; background: var(--ok); color: #fff;
            display: grid; place-items: center; font-size: 28px; margin: 0 auto 14px
        }

        /* form appears ONLY on mobile inside hero */
        .hero-form { display: none; margin-top: 30px; max-width: 460px }

        /* ---------- MODAL (desktop form) ---------- */
        .modal { position: fixed; inset: 0; z-index: 100; display: none; align-items: center; justify-content: center; padding: 20px }
        .modal.open { display: flex }
        .modal-backdrop { position: absolute; inset: 0; background: rgba(15, 8, 25, .62); backdrop-filter: blur(3px) }
        .modal-card { position: relative; z-index: 1; width: 100%; max-width: 440px; max-height: 92vh; overflow: auto }
        .modal-close {
            position: absolute; top: 12px; right: 12px; z-index: 2; width: 34px; height: 34px; border-radius: 50%;
            border: none; background: var(--parchment); color: var(--ink); font-size: 20px; cursor: pointer; line-height: 1;
        }
        .modal-close:hover { background: var(--line) }

        /* ---------- APPROVALS CAROUSEL ---------- */
        .approvals { background: #eef1f6; padding: 70px 0 78px; overflow: hidden }
        .approvals .appr-title { font-family: "Plus Jakarta Sans", sans-serif; font-weight: 800; font-size: clamp(30px, 4vw, 44px); text-align: center; color: var(--ink); margin-bottom: 20px }
        .approvals .appr-sub { text-align: center; font-family: "Plus Jakarta Sans", sans-serif; font-weight: 700; font-size: clamp(17px, 2.2vw, 21px); color: var(--ink); margin-bottom: 10px }
        .approvals .appr-lead { text-align: center; color: var(--ink-2); font-size: clamp(15px, 2vw, 17px); max-width: 780px; margin: 0 auto 40px }
        .appr-carousel { overflow: hidden; -webkit-mask: linear-gradient(90deg, transparent, #000 5%, #000 95%, transparent); mask: linear-gradient(90deg, transparent, #000 5%, #000 95%, transparent) }
        .appr-track { display: flex; gap: 24px; width: max-content; padding: 6px 0; animation: apprScroll 36s linear infinite }
        .appr-carousel:hover .appr-track { animation-play-state: paused }
        .appr-card { flex: 0 0 auto; width: 252px; background: #fff; border-radius: 16px; padding: 34px 24px; text-align: center; box-shadow: 0 14px 34px -20px rgba(35, 20, 54, .4); border: 1px solid #eceef3 }
        .appr-badge { width: 92px; height: 92px; margin: 0 auto 22px; border-radius: 50%; border: 1px solid #f0e2c8; display: grid; place-items: center; background: #fff }
        .appr-badge img { width: 66px; height: 66px; object-fit: contain }
        .appr-badge .ph { font-family: "Fraunces", serif; font-weight: 700; color: var(--maroon); font-size: 22px }
        .appr-card p { font-size: 15px; font-weight: 700; color: var(--ink); line-height: 1.35 }
        @keyframes apprScroll { to { transform: translateX(-50%) } }

        /* ---------- FEATURES (reasons) ---------- */
        .features { background: #f2f4f7 }
        .features .reasons-title { font-family: "Plus Jakarta Sans", sans-serif; font-weight: 800; text-align: center; font-size: clamp(25px, 3.6vw, 40px); line-height: 1.15; color: var(--ink); max-width: 920px; margin: 0 auto 46px }
        .features .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 26px }
        .feat { background: #fff; border: 1px solid #e9ebf0; border-radius: 16px; padding: 32px 30px; box-shadow: 0 14px 34px -22px rgba(35, 20, 54, .35); display: flex; flex-direction: column; transition: transform .2s, box-shadow .2s }
        .feat:hover { transform: translateY(-4px); box-shadow: 0 22px 44px -24px rgba(35, 20, 54, .4) }
        .feat .ic { width: 46px; height: 46px; margin-bottom: 18px; color: #1a1420 }
        .feat .ic svg { width: 100%; height: 100%; display: block }
        .feat h3 { font-family: "Plus Jakarta Sans", sans-serif; font-weight: 800; font-size: 20px; margin-bottom: 14px; color: var(--ink); line-height: 1.25 }
        .feat p { color: var(--slate); font-size: 15px; margin-bottom: 14px }
        .feat .btn { width: 100%; margin-top: auto }

        /* ---------- PROGRAM SPLIT ---------- */
        .program { background: linear-gradient(180deg, #fff, var(--parchment)) }
        .program .grid { display: grid; grid-template-columns: .9fr 1.1fr; gap: 44px; align-items: center }
        .cert-frame { border-radius: var(--r); overflow: hidden; box-shadow: var(--shadow); border: 6px solid #fff; background: #fff }
        .cert-frame img { width: 100%; height: 500px }
        .cert-ph { aspect-ratio: 4/3; display: grid; place-items: center; background: repeating-linear-gradient(45deg, #f6efe4, #f6efe4 12px, #f1e7d6 12px, #f1e7d6 24px); color: var(--maroon); font-family: "Fraunces", serif; font-size: 18px; text-align: center; padding: 20px }
        .program h2 { font-size: clamp(26px, 3.4vw, 36px); margin-bottom: 14px }
        .program ul { list-style: none; display: grid; gap: 12px; margin-top: 8px }
        .program li { display: flex; gap: 12px; font-size: 15px; color: var(--ink-2) }
        .program li::before { content: "◆"; color: var(--gold-deep); font-size: 12px; margin-top: 5px }

        /* ---------- CARDS ---------- */
        .cards { display: grid; grid-template-columns: repeat(4, 1fr); gap: 22px }
        .cards.spec { grid-template-columns: repeat(3, 1fr) }
        .course { background: var(--card); border: 1px solid var(--line); border-radius: var(--r); overflow: hidden; box-shadow: var(--shadow-sm); transition: transform .2s, box-shadow .2s; display: flex; flex-direction: column }
        .course:hover { transform: translateY(-5px); box-shadow: var(--shadow) }
        .course .thumb { aspect-ratio: 16/10; overflow: hidden; background: linear-gradient(140deg, var(--ink), var(--maroon)); position: relative }
        .course .thumb img { width: 100%; height: 100%; object-fit: cover }
        .course .thumb .fallback { position: absolute; inset: 0; display: grid; place-items: center; color: #fff; font-family: "Fraunces", serif; font-size: 18px; text-align: center; padding: 16px }
        .course .body { padding: 20px; display: flex; flex-direction: column; flex: 1 }
        .course h4 { font-size: 19px; margin-bottom: 12px }
        .course .meta { display: grid; gap: 7px; margin-bottom: 16px; font-size: 13.5px; color: var(--slate) }
        .course .meta div { display: flex; justify-content: space-between; border-bottom: 1px dashed var(--line); padding-bottom: 6px }
        .course .meta b { color: var(--ink) }
        .course .fee { color: var(--maroon); font-weight: 800 }
        .course .acts { display: flex; gap: 10px; margin-top: auto }
        .course .acts .btn { flex: 1; padding: 10px 12px; font-size: 13.5px }

        /* ---------- PROCESS ---------- */
        .process { background: var(--ink); color: #fff; overflow: hidden }
        .process .sec-head h2 { color: #fff }
        .process .sec-head p { color: rgba(255, 255, 255, .7) }
        .process .eyebrow { color: var(--gold) }
        .steps { display: grid; grid-template-columns: repeat(4, 1fr); gap: 22px; position: relative }
        .step { background: rgba(255, 255, 255, .05); border: 1px solid rgba(255, 255, 255, .12); border-radius: var(--r); padding: 26px 22px; position: relative }
        .step .num { font-family: "Fraunces", serif; font-size: 44px; color: var(--gold); line-height: 1; margin-bottom: 12px }
        .step h4 { color: #fff; font-size: 19px; margin-bottom: 8px }
        .step p { color: rgba(255, 255, 255, .72); font-size: 14px }

        /* ---------- HIRING ---------- */
        .hiring { padding: 56px 0; text-align: center }
        .hiring h2 { font-size: clamp(24px, 3vw, 32px); margin-bottom: 6px }
        .hiring .eyebrow { display: block; margin-bottom: 10px }
        .marquee { margin-top: 34px; overflow: hidden; -webkit-mask: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent); mask: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent) }
        .marquee-track { display: flex; gap: 56px; width: max-content; animation: scroll 26s linear infinite }
        .marquee:hover .marquee-track { animation-play-state: paused }
        .marquee img { height: 44px; width: auto; object-fit: contain; filter: grayscale(1); opacity: .7; transition: .2s }
        .marquee img:hover { filter: none; opacity: 1 }
        .logo-ph { height: 44px; min-width: 120px; display: grid; place-items: center; background: #fff; border: 1px solid var(--line); border-radius: 10px; font-weight: 800; color: var(--slate); padding: 0 18px; font-size: 14px }
        @keyframes scroll { to { transform: translateX(-50%) } }

        /* ---------- FAQ ---------- */
        .faq .grid { max-width: 820px; margin: 0 auto; display: grid; gap: 12px }
        .qa { background: var(--card); border: 1px solid var(--line); border-radius: 14px; overflow: hidden }
        .qa button { width: 100%; text-align: left; background: none; border: none; cursor: pointer; padding: 18px 22px; font-family: "Fraunces", serif; font-size: 17px; font-weight: 600; color: var(--ink); display: flex; justify-content: space-between; align-items: center; gap: 14px }
        .qa button:focus-visible { outline: 2px solid var(--gold); outline-offset: -2px }
        .qa .chev { flex: 0 0 auto; width: 26px; height: 26px; border-radius: 50%; background: var(--parchment); display: grid; place-items: center; color: var(--maroon); transition: transform .3s; font-weight: 800 }
        .qa.open .chev { transform: rotate(45deg) }
        .qa .ans { max-height: 0; overflow: hidden; transition: max-height .35s ease }
        .qa .ans p { padding: 0 22px 20px; color: var(--slate); font-size: 14.5px }

        /* ---------- CTA BAND ---------- */
        .cta-band { background: linear-gradient(120deg, var(--maroon), var(--ink)); color: #fff; text-align: center; border-radius: 26px; padding: 52px 28px; box-shadow: var(--shadow) }
        .cta-band h2 { font-size: clamp(26px, 3.6vw, 38px); margin-bottom: 10px }
        .cta-band p { color: rgba(255, 255, 255, .82); max-width: 520px; margin: 0 auto 22px }
        .cta-band .btns { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap }

        /* ---------- FOOTER ---------- */
        footer { background: #1b1029; color: rgba(255, 255, 255, .8); padding: 56px 0 30px; margin-top: 20px }
        .foot-grid { display: grid; grid-template-columns: 1.4fr 1fr 1fr; gap: 36px; margin-bottom: 34px }
        footer h4 { color: #fff; font-size: 16px; margin-bottom: 14px; font-family: "Plus Jakarta Sans"; font-weight: 700 }
        .foot-brand img { height: 40px; margin-bottom: 14px }
        .foot-brand .fallback { font-family: "Fraunces", serif; font-size: 22px; color: var(--gold); margin-bottom: 10px }
        .foot-brand p { font-size: 13.5px; max-width: 320px }
        footer ul { list-style: none; display: grid; gap: 9px }
        footer ul a { font-size: 14px; opacity: .8 }
        footer ul a:hover { opacity: 1; color: var(--gold) }
        .queries { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 8px }
        .queries span { font-size: 11.5px; background: rgba(255, 255, 255, .06); border: 1px solid rgba(255, 255, 255, .1); padding: 5px 10px; border-radius: 999px; font-family: "IBM Plex Mono", monospace; opacity: .75 }
        .disclaimer { border-top: 1px solid rgba(255, 255, 255, .12); padding-top: 22px; font-size: 12.5px; color: rgba(255, 255, 255, .6); line-height: 1.7 }
        .disclaimer b { color: rgba(255, 255, 255, .85) }
        .copy { margin-top: 18px; font-size: 12.5px; display: flex; justify-content: space-between; flex-wrap: wrap; gap: 10px; color: rgba(255, 255, 255, .55) }

        /* ---------- FLOATING (pill buttons) ---------- */
        .floaties { position: fixed; right: 18px; bottom: 18px; z-index: 70; display: flex; flex-direction: column; gap: 12px; align-items: flex-end }
        .fab {
            display: inline-flex; align-items: center; gap: 10px; padding: 10px 20px 10px 11px;
            border-radius: 999px; color: #fff; font-family: "Plus Jakarta Sans", sans-serif; font-weight: 700;
            font-size: 16px; border: none; cursor: pointer; text-decoration: none;
            box-shadow: 0 12px 26px -8px rgba(0, 0, 0, .38); transition: transform .15s ease, box-shadow .2s ease;
        }
        .fab:hover { transform: translateY(-2px); box-shadow: 0 18px 32px -10px rgba(0, 0, 0, .45) }
        .fab:focus-visible { outline: 3px solid rgba(255, 255, 255, .7); outline-offset: 2px }
        .fab .fab-ic { width: 30px; height: 30px; border-radius: 50%; background: rgba(255, 255, 255, .22); display: grid; place-items: center; flex: 0 0 auto }
        .fab .fab-ic svg { width: 17px; height: 17px; display: block }
        .fab.call { background: #159bb0 }
        .fab.wa { background: #25d366 }
        .mobile-bar { display: none }

        /* ---------- REVEAL ---------- */
        .reveal { opacity: 0; transform: translateY(24px); transition: opacity .6s ease, transform .6s ease }
        .reveal.in { opacity: 1; transform: none }
        @media (prefers-reduced-motion:reduce) {
            .reveal { opacity: 1; transform: none; transition: none }
            .marquee-track, .appr-track { animation: none }
            .appr-track { flex-wrap: wrap; justify-content: center }
            html { scroll-behavior: auto }
        }

        /* ---------- RESPONSIVE ---------- */
        @media (max-width:1000px) {
            .cards { grid-template-columns: repeat(2, 1fr) }
            .cards.spec { grid-template-columns: repeat(2, 1fr) }
            .steps { grid-template-columns: repeat(2, 1fr) }
            .program .grid { grid-template-columns: 1fr; gap: 30px }
            .foot-grid { grid-template-columns: 1fr 1fr }
        }

        @media (max-width:760px) {
            .nav-links, .nav-cta .btn { display: none }
            .hamburger { display: block }
            .nav-links {
                position: absolute; top: 72px; left: 0; right: 0; flex-direction: column; gap: 0;
                background: var(--parchment); border-bottom: 1px solid var(--line);
                padding: 8px 20px 20px; box-shadow: var(--shadow); display: none;
            }
            .nav-links.show { display: flex }
            .nav-links li { width: 100% }
            .nav-links a { display: block; padding: 14px 0; border-bottom: 1px solid var(--line); width: 100% }

            /* hero tweaks for mobile + reveal the form */
            .hero { min-height: auto }
            .hero-overlay { background: linear-gradient(180deg, rgba(8, 10, 25, .62), rgba(8, 10, 25, .88)) }
            .hero-bg { background-position: center }
            .hero-inner { padding: 44px 20px 40px }
            .hero-form { display: block }
            .hero-cta .btn { width: 100% }

            section { padding: 56px 0 }
            .approvals { padding: 54px 0 60px }
            .appr-card { width: 210px; padding: 28px 20px }
            .appr-badge { width: 80px; height: 80px }
            .appr-badge img { width: 56px; height: 56px }
            .features .grid { grid-template-columns: 1fr }
            .cards, .cards.spec { grid-template-columns: 1fr }
            .steps { grid-template-columns: 1fr }
            .foot-grid { grid-template-columns: 1fr }
            .floaties { display: flex }
            .floaties .fab.call { display: none }
            .floaties { bottom: 78px }
            .mobile-bar {
                display: flex; position: fixed; left: 0; right: 0; bottom: 0; z-index: 70; background: #fff;
                border-top: 1px solid var(--line); padding: 9px 12px; gap: 10px; box-shadow: 0 -8px 24px -18px rgba(0, 0, 0, .4)
            }
            .mobile-bar .btn { flex: 1; padding: 12px }
            body { padding-bottom: 70px }
        }

        @media (max-width:420px) {
            .cd-box { min-width: 50px; height: 50px }
            .cd-box span { font-size: 21px }
            .lead-perks { gap: 8px; flex-wrap: nowrap }
            .lead-perks span { font-size: 12px; white-space: nowrap }
        }
        .field input,
        .field select,
        .field textarea {
            width: 100%;
            padding: 11px 15px;
            border: 1.5px solid var(--line);
            border-radius: 12px;
            font-family: inherit;   /* fixes the monospace look */
            font-size: 15px;
            color: var(--ink);
            background: #fdfbf7;
            transition: border .15s;
        }

        .field textarea {
            resize: vertical;       /* only drag up/down, not sideways */
            min-height: 84px;
            line-height: 1.5;
        }

        .field input:focus,
        .field select:focus,
        .field textarea:focus {
            outline: none;
            border-color: var(--maroon);
        }

        .field.invalid input,
        .field.invalid select,
        .field.invalid textarea {
            border-color: var(--maroon);
        }

        .lead-perks { margin-bottom: 10px }
    </style>
</head>

<body>
    <!-- ============ HEADER ============ -->
    <header id="top">
        <div class="wrap nav">
            <a class="brand" href="#top" aria-label="Home">
                <img src="images/logo.webp" alt="IGNOU Online" onerror="this.style.display='none';this.nextElementSibling.style.display='inline'">
                <span class="fallback" style="display:none">IGNOU&nbsp;Online</span>
            </a>
            <ul class="nav-links" id="navLinks">
                <li><a href="#program">Program</a></li>
                <li><a href="#courses">Courses</a></li>
                <li><a href="#process">Admission</a></li>
                <li><a href="#specializations">Specializations</a></li>
                <li><a href="#faq">FAQ</a></li>
            </ul>
            <div class="nav-cta">
                <a class="btn btn-gold" href="#counselling">Apply Now</a>
                <button class="hamburger" id="hamburger" aria-label="Menu" aria-expanded="false">
                    <span></span><span></span><span></span>
                </button>
            </div>
        </div>
    </header>

    <!-- ============ HERO (screenshot style) ============ -->
    <section class="hero">
        <div class="hero-bg" role="img" aria-label="IGNOU campus"></div>
        <div class="hero-overlay"></div>
        <div class="wrap hero-inner">
            <div class="hero-copy reveal">
                <h1>IGNOU Online MBA 2026</h1>

                
                <p class="naac">University Certified by NAAC with A++ Grade</p>

                <div class="hero-cta">
                    <a class="btn btn-gold" href="#counselling">Download Brochure&nbsp;&#8595;</a>
                    <a class="btn btn-indigo" href="#counselling">Get Free counselling&nbsp;&#8594;</a>
                </div>

                <p class="scholarship-text">Get Upto ₹10,000 Scholarship Benefit</p>
                <div class="course-pills">
                    <span class="pill">MBA</span>
                    <span class="pill">MCA</span>
                    <span class="pill">MA</span>
                    <span class="pill">BBA</span>
                    <span class="pill">BCA</span>
                    <span class="pill">B.Sc</span>
                    <span class="pill">B.Com</span>
                </div>
                <p class="closing">Admissions closing soon!</p>

                <div class="countdown" aria-label="Time left to apply">
                    <div class="cd-unit"><div class="cd-box"><span id="cd-d">01</span></div><small>DAY</small></div>
                    <span class="cd-colon">:</span>
                    <div class="cd-unit"><div class="cd-box"><span id="cd-h">23</span></div><small>HRS</small></div>
                    <span class="cd-colon">:</span>
                    <div class="cd-unit"><div class="cd-box"><span id="cd-m">11</span></div><small>MINS</small></div>
                    <span class="cd-colon">:</span>
                    <div class="cd-unit"><div class="cd-box"><span id="cd-s">00</span></div><small>SECS</small></div>
                </div>
            </div>

            <!-- FORM: only visible on mobile -->
            <div class="lead-card hero-form reveal" id="counselling" data-form-card></div>
        </div>
    </section>

    <!-- ============ APPROVALS CAROUSEL ============ -->
    <section class="approvals">
        <div class="wrap reveal">
            <h2 class="appr-title">IGNOU Online MBA</h2>
            <p class="appr-sub">How About Getting an Online Degree From IGNOU?</p>
            <p class="appr-lead">IGNOU in itself is a self-governing authority directly under MHRD (Ministry of Human Resource Development)</p>
        </div>
        <div class="appr-carousel">
            <div class="appr-track" id="apprTrack"></div>
        </div>
    </section>

    <!-- ============ FEATURES (reasons) ============ -->
    <section class="features">
        <div class="wrap">
            <h2 class="reasons-title reveal">Reasons to pursue an Online MBA Degree from IGNOU</h2>
            <div class="grid">
                <div class="feat reveal">
                    <div class="ic">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M3 12h18"/><path d="M12 3a15 15 0 0 1 0 18"/><path d="M12 3a15 15 0 0 0 0 18"/></svg>
                    </div>
                    <h3>Accessible from all over the Globe</h3>
                    <p>The program offered by IGNOU is available in India and a few other countries.</p>
                    <p>You can pursue your degree from IGNOU, even if you are not residing in India, it is accessible to all.</p>
                    <a class="btn btn-gold" href="#counselling">Explore Benefits&nbsp;&#8594;</a>
                </div>
                <div class="feat reveal">
                    <div class="ic">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M22 10 12 5 2 10l10 5 10-5Z"/><path d="M6 12v5c0 1.2 2.7 2.6 6 2.6s6-1.4 6-2.6v-5"/><path d="M22 10v5"/></svg>
                    </div>
                    <h3>Latest Curriculum &amp; Study Material</h3>
                    <p>IGNOU offers a contemporary curriculum with cutting-edge study aids for excellence.</p>
                    <p>IGNOU offers affordable rates, ensuring that education is accessible to a larger audience.</p>
                    <a class="btn btn-gold" href="#counselling">Explore Program&nbsp;&#8594;</a>
                </div>
                <div class="feat reveal">
                    <div class="ic">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="2.6"/><circle cx="12" cy="4" r="1.7"/><circle cx="5" cy="17.5" r="1.7"/><circle cx="19" cy="17.5" r="1.7"/><path d="M12 6.6v2.8"/><path d="M10 13.6 6.4 16"/><path d="M14 13.6 17.6 16"/></svg>
                    </div>
                    <h3>Flexible learning</h3>
                    <p>IGNOU offers flexible schedules, allowing students to learn on their own pace.</p>
                    <p>The university's online platform facilitates self-directed and hands-on learning.</p>
                    <a class="btn btn-gold" href="#counselling">Start Learning&nbsp;&#8594;</a>
                </div>
            </div>
        </div>
    </section>

    <!-- ============ PROGRAM SPLIT ============ -->
    <section class="program" id="program">
        <div class="wrap grid">
            <div class="cert-frame reveal">
                <img src="images/IGNOU-Marksheet.jpg" alt="Sample IGNOU certificate" onerror="this.outerHTML='<div class=cert-ph>Sample Degree Certificate<br>IGNOU · NAAC A++</div>'">
            </div>
            <div class="reveal">
                <p class="eyebrow">The Programme</p>
                <h2>Online MBA program from IGNOU</h2>
                <p style="color:var(--slate);margin-bottom:6px">Benefit from e-lectures, academic counselling, career assistance and more.</p>
                <ul>
                    <li>Affordable fee structure for budget-friendly education</li>
                    <li>Flexible learning designed for working professionals</li>
                    <li>Academic guidance and counselling support</li>
                    <li>Access to digital study material &amp; e-library</li>
                    <li>Online classes, teleconferencing &amp; self-paced support</li>
                    <li>Recognised government university with nationwide credibility</li>
                </ul>
                <div class="hero-cta" style="margin-top:24px">
                    <a class="btn btn-ink" href="#counselling">Talk to a Counsellor</a>
                    <a class="btn btn-outline" href="#courses">See Fees</a>
                </div>
            </div>
        </div>
    </section>

    <!-- ============ COURSES ============ -->
    <section id="courses">
        <div class="wrap">
            <div class="sec-head reveal">
                <p class="eyebrow">PG Courses</p>
                <h2>Online programs</h2>
                <p>Pick the programme that matches your goals and schedule.</p>
            </div>
            <div class="cards" id="coursesGrid"></div>
        </div>
    </section>

    <!-- ============ PROCESS ============ -->
    <section class="process" id="process">
        <div class="wrap">
            <div class="sec-head reveal">
                <p class="eyebrow">4 Simple Steps</p>
                <h2>Admission process</h2>
                <p>From application to your first class — here's how it works.</p>
            </div>
            <div class="steps">
                <div class="step reveal"><div class="num">01</div><h4>Apply online</h4><p>Fill out the application form with your basic details.</p></div>
                <div class="step reveal"><div class="num">02</div><h4>Upload documents</h4><p>Submit the required academic and ID documents.</p></div>
                <div class="step reveal"><div class="num">03</div><h4>Pay fees</h4><p>Complete payment through the secure gateway.</p></div>
                <div class="step reveal"><div class="num">04</div><h4>Confirmation</h4><p>Admission confirmed — and you're ready to start learning.</p></div>
            </div>
            <div style="text-align:center;margin-top:40px" class="reveal">
                <a class="btn btn-gold" href="#counselling">Know the Fees — Click Here</a>
            </div>
        </div>
    </section>

    <!-- ============ SPECIALIZATIONS ============ -->
    <section id="specializations">
        <div class="wrap">
            <div class="sec-head reveal">
                <p class="eyebrow">Choose Your Path</p>
                <h2>Online MBA specializations</h2>
                <p>Tailor your MBA to the career you want to build.</p>
            </div>
            <div class="cards spec" id="specGrid"></div>
        </div>
    </section>

    <!-- ============ HIRING ============ -->
    <div class="hiring">
        <div class="wrap reveal">
            <span class="eyebrow">Career Outcomes</span>
            <h2>Our learners work at</h2>
        </div>
        <div class="marquee">
            <div class="marquee-track" id="marquee"></div>
        </div>
    </div>

    <!-- ============ FAQ ============ -->
    <section class="faq" id="faq">
        <div class="wrap">
            <div class="sec-head reveal">
                <p class="eyebrow">Need to Know</p>
                <h2>Frequently asked questions</h2>
                <p>Quick answers to common admission queries.</p>
            </div>
            <div class="grid" id="faqGrid"></div>
        </div>
    </section>

    <!-- ============ CTA BAND ============ -->
    <section>
        <div class="wrap">
            <div class="cta-band reveal">
                <h2>Ready to start your MBA journey?</h2>
                <p>Get free counselling, check fees and reserve your seat for the 2026 batch before admissions close.</p>
                <div class="btns">
                    <a class="btn btn-gold" href="#counselling">Get Free Counselling</a>
                    <a class="btn btn-ghost-light" href="#courses">Download Brochure</a>
                </div>
            </div>
        </div>
    </section>

    <!-- ============ FOOTER ============ -->
    <footer>
        <div class="wrap">
            <div class="foot-grid">
                <div class="foot-brand">
                    <img src="images/logo.webp" alt="IGNOU Online" onerror="this.outerHTML='<div class=fallback>IGNOU Online</div>'">
                    <p>An information partner helping learners explore IGNOU's online master's programmes, fees and admission process.</p>
                </div>
                <div>
                    <h4>Programs</h4>
                    <ul>
                        <li><a href="#courses">Online MBA</a></li>
                        <li><a href="#courses">1 Year MBA</a></li>
                        <li><a href="#courses">Online MCA</a></li>
                        <li><a href="#courses">Online M.Com</a></li>
                    </ul>
                </div>
                <div>
                    <h4>Quick Links</h4>
                    <ul>
                        <li><a href="#process">Admission Process</a></li>
                        <li><a href="#specializations">Specializations</a></li>
                        <li><a href="#faq">FAQs</a></li>
                        <li><a href="#counselling">Apply Now</a></li>
                    </ul>
                </div>
            </div>

            <h4 style="margin-bottom:12px">Top Queries</h4>
            <div class="queries" id="queries"></div>

            <div class="disclaimer" style="margin-top:28px">
                <b>Disclaimer:</b> We act solely as an information partner and do not conduct or facilitate admissions to IGNOU. For admissions, please visit the official IGNOU website (ignou.ac.in) or contact the university directly. IGNOU holds full rights to request changes or removal of any non-relevant content. Images are for illustrative purposes only and do not directly represent the respective colleges or universities.
            </div>
            <div class="copy">
                <span>© 2026 · All Rights Reserved.</span>
                <a href="#top">Privacy Policy</a>
            </div>
        </div>
    </footer>

    <!-- ============ FLOATING ACTIONS ============ -->
    <div class="floaties">
        <!-- <a class="fab call" href="tel:+910000000000" title="Call us">
            <span class="fab-ic">
                <svg viewBox="0 0 24 24" fill="currentColor"><path d="M6.6 10.8a15.5 15.5 0 0 0 6.6 6.6l2.2-2.2a1 1 0 0 1 1-.24 11.4 11.4 0 0 0 3.6.58 1 1 0 0 1 1 1V20a1 1 0 0 1-1 1A17 17 0 0 1 3 4a1 1 0 0 1 1-1h3.5a1 1 0 0 1 1 1 11.4 11.4 0 0 0 .58 3.6 1 1 0 0 1-.24 1L6.6 10.8Z"/></svg>
            </span>
            Call
        </a> -->
        <a class="fab wa" href="https://wa.me/+919218101960" target="_blank" rel="noopener" title="Chat on WhatsApp">
            <span class="fab-ic">
                <svg viewBox="0 0 24 24" fill="currentColor"><path d="M17.5 14.4c-.3-.15-1.7-.85-2-.95-.26-.1-.45-.15-.64.15-.19.28-.73.94-.9 1.13-.16.19-.33.21-.61.07a8.2 8.2 0 0 1-2.4-1.48 9 9 0 0 1-1.67-2.07c-.17-.3 0-.46.13-.6.13-.13.28-.33.42-.5.14-.17.19-.29.28-.48.1-.19.05-.36-.02-.5-.07-.15-.64-1.54-.88-2.1-.23-.55-.46-.48-.64-.49h-.55c-.19 0-.5.07-.76.36-.26.28-1 .98-1 2.4 0 1.42 1.02 2.78 1.16 2.97.14.19 2.02 3.08 4.9 4.32.68.29 1.22.47 1.63.6.69.22 1.31.19 1.8.11.55-.08 1.7-.69 1.94-1.36.24-.67.24-1.24.17-1.36-.07-.12-.26-.19-.55-.33ZM12 2a10 10 0 0 0-8.5 15.28L2 22l4.84-1.27A10 10 0 1 0 12 2Zm0 18.2a8.2 8.2 0 0 1-4.18-1.14l-.3-.18-2.87.75.77-2.8-.2-.31A8.2 8.2 0 1 1 12 20.2Z"/></svg>
            </span>
            Chat
        </a>
    </div>
    <div class="mobile-bar">
        <a class="btn btn-outline" href="#counselling">Brouture</a>
        <a class="btn btn-gold" href="#counselling">Apply Now</a>
    </div>

    <!-- ============ DESKTOP FORM MODAL ============ -->
    <div class="modal" id="formModal" aria-hidden="true" role="dialog" aria-modal="true">
        <div class="modal-backdrop" data-close-modal></div>
        <div class="lead-card modal-card">
            <button class="modal-close" data-close-modal aria-label="Close">×</button>
            <div id="modalFormMount"></div>
        </div>
    </div>

    <script>
        /* ---------- DATA ---------- */
        const courses = [
            {img:"online-mba.png", name:"Online MBA", elig:"Bachelor's Degree", dur:"2 Years", fee:"₹62,000"},
            {img:"online-mca.png", name:"Online MCA", elig:"BCA / B.Sc. Degree", dur:"2 Years", fee:"₹52,000"},
            {img:"1-year-online-mba.png", name:"Online MA", elig:"Bachelor's Degree", dur:"2 Year", fee:"₹40,000"},            
            {img:"online-mcom.png", name:"Online BBA", elig:"12th Pass", dur:"3 Years", fee:"₹16,200"},
            {img:"1-year-online-mba.png", name:"Online BCA", elig:"12th Pass", dur:"3 Years", fee:"₹16,200"},
            {img:"online-mcom.png", name:"Online B.Com", elig:"12th Pass", dur:"3 Years", fee:"₹16,200"},
            {img:"1-year-online-mba.png", name:"Online B.Sc", elig:"12th Pass", dur:"3 Years", fee:"₹16,200"},
        ];
        const specs = [
            {img:"u1.jpg", name:"Financial Management"},
            {img:"u2.jpg", name:"Marketing Management"},
            {img:"u3.jpg", name:"Human Resource Management"},
            {img:"p1.jpg", name:"Operations Management"},
            {img:"p2.jpg", name:"Data Science & Analytics"},
        ];
        const faqs = [
            ["Is the IGNOU Online MBA valid and recognised?","Yes. The programme is approved by UGC-DEB and widely recognised across India. As a government university, the degree is valid for both private and public sector opportunities."],
            ["What is the eligibility for the Online MBA?","Candidates need a bachelor's degree in any discipline from a recognised university. Some specializations may require minimum marks as per university guidelines."],
            ["What is the total fee for the Online MBA?","The fee is comparatively affordable and can be paid semester-wise. Exact fees may vary slightly, so it's best to check the latest structure during admission."],
            ["What is the duration of the programme?","The minimum duration is 2 years, with flexibility to complete it within a maximum period as per university norms."],
            ["Are there live classes or recorded lectures?","There's a mix of recorded lectures, study material and online support, with some guidance provided through virtual sessions."],
            ["Is placement support available?","Placement assistance is offered, and the degree holds value for career growth, promotions and government job eligibility."],
            ["Can working professionals apply?","Yes. The programme is designed for working professionals and lets you study at your own pace without affecting your job."],
        ];
        const queries = ["ignou online mba","mba admission 2026","online mba fees","mba eligibility","mba course details","apply online","admission last date","mba for working professionals","mba specializations","fees structure","registration online","admission without entrance"];
        const base = "images/";

        /* ---------- REUSABLE FORM MARKUP ---------- */
        function formHTML() {
            return `
            <div class="form-view">
                <h3>Get a Free Career Counselling Session</h3>
                <p class="sub">Speak to an admission counsellor today.</p>
                <div class="lead-perks"><span>No-Cost EMIs</span><span>100% Online Exams</span></div>
                <form class="leadForm" action="submit.php" method="post" novalidate>
                    <div class="field"><input type="text" name="name" placeholder="Full name" autocomplete="name"><div class="err">Please enter your name.</div></div>
                    <div class="field"><input type="tel" name="contactno" placeholder="Phone number" autocomplete="tel" inputmode="numeric"><div class="err">Enter a valid 10-digit phone number.</div></div>
                    <div class="field"><input type="email" name="email" placeholder="Email address" autocomplete="email"><div class="err">Enter a valid email address.</div></div>
                    <div class="field">
                        <select name="selectcourse">
                            <option value="">Choose a course</option>
                            <option>MBA</option><option>MCA</option><option>MA</option><option>BBA</option><option>BCA</option><option>B.Com</option>
                        </select>
                        <div class="err">Please select a course.</div>
                    </div>
                    <div class="field"><textarea name="message" rows="2" placeholder="Your message (optional)"></textarea></div>
                    <input type="text" name="website" tabindex="-1" autocomplete="off" aria-hidden="true" style="position:absolute;left:-9999px;width:1px;height:1px;opacity:0">
                    <button type="submit" class="btn btn-gold">Submit &amp; Get a Callback</button>
                    <p class="form-note">By submitting you agree to be contacted about admissions.</p>
                </form>
            </div>
            <div class="form-success">
                <div class="tick">✓</div>
                <h3>Thank you!</h3>
                <p class="sub">A counsellor will call you shortly to guide you through the process.</p>
                <button class="btn btn-outline reset-lead" type="button">Submit another enquiry</button>
            </div>`;
        }

        /* mount form into mobile hero card + desktop modal */
        document.querySelector('[data-form-card]').innerHTML = formHTML();
        document.getElementById('modalFormMount').innerHTML = formHTML();

        /* ---------- FORM VALIDATION + SUBMIT TO submit.php ---------- */
        function attachForm(cardEl) {
            const form = cardEl.querySelector('.leadForm');
            if (!form) return;
            const btn = form.querySelector('button[type="submit"]');
            form.addEventListener('submit', e => {
                let ok = true;
                const f = form.elements;
                const set = (el, valid) => { el.closest('.field').classList.toggle('invalid', !valid); if (!valid) ok = false; };

                // strip spaces/dashes so the server's 10-digit check passes
                const digits = f.contactno.value.replace(/\D/g, '');
                f.contactno.value = digits;

                set(f.name, f.name.value.trim().length > 1);
                set(f.contactno, /^\d{10}$/.test(digits));
                set(f.selectcourse, f.selectcourse.value !== '');
                // email is optional in your PHP; validate only if filled
                set(f.email, f.email.value.trim() === '' || /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(f.email.value));

                if (!ok) { e.preventDefault(); return; }        // block bad submits
                btn.disabled = true; btn.textContent = 'Submitting…'; // valid → native POST proceeds
            });
            form.querySelectorAll('input,select,textarea').forEach(el =>
                el.addEventListener('input', () => el.closest('.field').classList.remove('invalid')));
            const reset = cardEl.querySelector('.reset-lead');
            if (reset) reset.addEventListener('click', () => {
                form.reset();
                cardEl.querySelector('.form-success')?.classList.remove('show');
                cardEl.querySelector('.form-view')?.classList.remove('hide');
            });
        }
        document.querySelectorAll('[data-form-card], .modal-card').forEach(attachForm);

        /* ---------- DESKTOP MODAL / MOBILE SCROLL ROUTING ---------- */
        const modal = document.getElementById('formModal');
        const isMobile = () => window.matchMedia('(max-width:760px)').matches;
        function openModal() { modal.classList.add('open'); modal.setAttribute('aria-hidden', 'false'); document.body.style.overflow = 'hidden'; }
        function closeModal() { modal.classList.remove('open'); modal.setAttribute('aria-hidden', 'true'); document.body.style.overflow = ''; }
        modal.querySelectorAll('[data-close-modal]').forEach(el => el.addEventListener('click', closeModal));
        document.addEventListener('keydown', e => { if (e.key === 'Escape') closeModal(); });

        /* every "#counselling" link: desktop -> modal, mobile -> scroll to inline form */
        document.addEventListener('click', e => {
            const a = e.target.closest('a[href="#counselling"]');
            if (!a) return;
            if (!isMobile()) { e.preventDefault(); openModal(); }
            else {
                links.classList.remove('show'); ham.classList.remove('open');
                document.getElementById('counselling').scrollIntoView({ behavior: 'smooth', block: 'center' });
            }
        });

        /* ---------- COUNTDOWN (rolling, never negative) ---------- */
        let target = new Date(); target.setDate(target.getDate() + 2); target.setHours(23, 59, 59, 0);
        const pad = n => String(n).padStart(2, '0');
        function tick() {
            let diff = target - new Date();
            if (diff < 0) { target.setDate(target.getDate() + 7); diff = target - new Date(); }
            const d = Math.floor(diff / 86400000);
            const h = Math.floor(diff % 86400000 / 3600000);
            const m = Math.floor(diff % 3600000 / 60000);
            const s = Math.floor(diff % 60000 / 1000);
            document.getElementById('cd-d').textContent = pad(d);
            document.getElementById('cd-h').textContent = pad(h);
            document.getElementById('cd-m').textContent = pad(m);
            document.getElementById('cd-s').textContent = pad(s);
        }
        tick(); setInterval(tick, 1000);

        /* ---------- RENDER COURSES ---------- */
        document.getElementById("coursesGrid").innerHTML = courses.map(c => `
        <div class="course reveal">
            <div class="thumb"><img src="${base}${c.img}" alt="${c.name}" onerror="this.outerHTML='<div class=fallback>${c.name}</div>'"></div>
            <div class="body">
                <h4>${c.name}</h4>
                <div class="meta">
                    <div><span>Eligibility</span><b>${c.elig}</b></div>
                    <div><span>Duration</span><b>${c.dur}</b></div>
                    <div><span>Fee</span><b class="fee">${c.fee}</b></div>
                </div>
                <div class="acts">
                    <a class="btn btn-outline" href="#counselling">Brochure</a>
                    <a class="btn btn-ink" href="#counselling">Enquire</a>
                </div>
            </div>
        </div>`).join("");

        /* ---------- RENDER SPECIALIZATIONS ---------- */
        document.getElementById("specGrid").innerHTML = specs.map(s => `
        <div class="course reveal">
            <div class="thumb"><img src="${base}${s.img}" alt="MBA in ${s.name}" onerror="this.outerHTML='<div class=fallback>MBA in ${s.name}</div>'"></div>
            <div class="body">
                <h4>MBA in ${s.name}</h4>
                <div class="meta">
                    <div><span>Eligibility</span><b>Bachelor's Degree</b></div>
                    <div><span>Duration</span><b>2 Years</b></div>
                    <div><span>Fee</span><b class="fee">₹62,000</b></div>
                </div>
                <div class="acts">
                    <a class="btn btn-outline" href="#counselling">Brochure</a>
                    <a class="btn btn-ink" href="#counselling">View Details →</a>
                </div>
            </div>
        </div>`).join("");

        /* ---------- RENDER APPROVALS CAROUSEL ---------- */
        const apprItems = [
            {img:"a3.png", label:"NAAC with A++", ph:"A+"},
            {img:"a4.png", label:"Approved by UGC (University Grants Commission)", ph:"UGC"},
            {img:"a1.png", label:"Approved by the Association of Indian Universities (AIU)", ph:"AIU"},
            {img:"a2.png", label:"Approved by All India Council for Technical Education (AICTE)", ph:"AICTE"},
            {img:"a3.png", label:"NAAC with A++", ph:"A+"},
        ];
        const apprCards = apprItems.map(a => `
        <div class="appr-card">
            <div class="appr-badge"><img src="${base}${a.img}" alt="${a.label}" onerror="this.outerHTML='<div class=ph>${a.ph}</div>'"></div>
            <p>${a.label}</p>
        </div>`).join("");
        document.getElementById("apprTrack").innerHTML = apprCards + apprCards;

        /* ---------- RENDER MARQUEE ---------- */
        const logos = [1,2,3,4,5,6].map(n => `<img src="${base}hiring${n}.png" alt="Hiring partner" onerror="this.outerHTML='<div class=logo-ph>Partner ${n}</div>'">`).join("");
        document.getElementById("marquee").innerHTML = logos + logos;

        /* ---------- RENDER FAQ ---------- */
        document.getElementById("faqGrid").innerHTML = faqs.map(([q, a]) => `
        <div class="qa reveal">
            <button aria-expanded="false"><span>${q}</span><span class="chev">+</span></button>
            <div class="ans"><p>${a}</p></div>
        </div>`).join("");
        document.querySelectorAll(".qa button").forEach(btn => {
            btn.addEventListener("click", () => {
                const qa = btn.parentElement, ans = qa.querySelector(".ans"), open = qa.classList.contains("open");
                document.querySelectorAll(".qa").forEach(o => { o.classList.remove("open"); o.querySelector(".ans").style.maxHeight = null; o.querySelector("button").setAttribute("aria-expanded", "false"); });
                if (!open) { qa.classList.add("open"); ans.style.maxHeight = ans.scrollHeight + "px"; btn.setAttribute("aria-expanded", "true"); }
            });
        });

        /* ---------- RENDER QUERIES ---------- */
        document.getElementById("queries").innerHTML = queries.map(q => `<span>${q}</span>`).join("");

        /* ---------- MOBILE NAV ---------- */
        const ham = document.getElementById("hamburger"), links = document.getElementById("navLinks");
        ham.addEventListener("click", () => {
            const open = links.classList.toggle("show");
            ham.classList.toggle("open", open);
            ham.setAttribute("aria-expanded", open);
        });
        links.querySelectorAll("a").forEach(a => a.addEventListener("click", () => {
            links.classList.remove("show"); ham.classList.remove("open"); ham.setAttribute("aria-expanded", "false");
        }));

        /* ---------- STICKY HEADER SHADOW ---------- */
        addEventListener("scroll", () => document.querySelector("header").classList.toggle("scrolled", scrollY > 8));

        /* ---------- SCROLL REVEAL ---------- */
        const io = new IntersectionObserver(es => es.forEach(e => { if (e.isIntersecting) { e.target.classList.add("in"); io.unobserve(e.target); } }), { threshold: .12 });
        document.querySelectorAll(".reveal").forEach(el => io.observe(el));

        /* ---------- AUTO-OPEN FORM AFTER 30s (once per session) ---------- */
        let autoPopupDone = false;

        // if user opens the form themselves, cancel the auto-popup
        document.addEventListener('click', e => {
            if (e.target.closest('a[href="#counselling"]')) autoPopupDone = true;
        });

        if (!sessionStorage.getItem('popupShown')) {
            setTimeout(() => {
                if (autoPopupDone || modal.classList.contains('open')) return;
                sessionStorage.setItem('popupShown', '1');
                if (isMobile()) {
                    document.getElementById('counselling').scrollIntoView({ behavior: 'smooth', block: 'center' });
                } else {
                    openModal();
                }
            }, 30000);
        }
    </script>
</body>

</html>
